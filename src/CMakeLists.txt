#set(SOURCE_FILES base64.cpp)
add_library(crypto ${SOURCE_FILES})

target_include_directories(crypto PRIVATE ${CMAKE_SOURCE_DIR}/inc/)
#target_compile_features(crypto PRIVATE cxx_range_for)
set_target_properties(crypto PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR}/bin/)
